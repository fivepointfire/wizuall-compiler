// Test custom helpers: runningSum, pairwiseCompare, paretoSet

nums = [1, 2, 2, 3, 4, 4, 5];

cumsum = runningSum(nums);
diffs = pairwiseCompare(nums);
unique = paretoSet(nums);

print("cumsum:", cumsum);
print("diffs:", diffs);
print("unique:", unique);